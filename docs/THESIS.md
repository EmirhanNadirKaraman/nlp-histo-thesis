# Thesis Notebook — Issues found, decisions made

A running ledger of substantive issues discovered in the `nlp-histo` pipeline,
the diagnosis trail for each, the fix shipped, and the artifacts that
demonstrate the before/after behaviour. Reproduction commands live in
[`HOW_TO_RUN.md`](HOW_TO_RUN.md); code layout lives in
[`STRUCTURE.md`](STRUCTURE.md); the bug catalogue and per-bug write-ups live
in [`BUGS.md`](BUGS.md).

> **How to use this file:**
> * [Bugs](BUGS.md) — every substantive defect, catalogue + per-bug detail.
> * [TODOs](#todos) — work to carry forward into the thesis or follow-up commits.
> * [Decisions log](#decisions-log) — non-obvious calls made along the way.
>
> Add a new entry the same day the issue is discovered. New bug → row in
> [`BUGS.md`](BUGS.md) catalogue + detail section. New TODO → bullet in the
> TODO list below (with the bug or section it belongs to). Never delete
> entries — flip `Status` to `Fixed` / `Won't fix` / `Superseded` so the
> history survives.

---

## TODOs

Carry-forward items. Tick `- [x]` when shipped, then move the entry to the
"Decisions log" if it represents a permanent decision, or to the matching Bug
detail section in [`BUGS.md`](BUGS.md) if it closes a bug.

* [ ] **B-001 follow-up:** regenerate `out/summaries/runs/*/` artifacts for papers that participated in a corpus-relate run *before* the `pmcid`-in-`group_id` fix — their `canonical_id`s are still colliding values on disk.
* [ ] **B-001 defence-in-depth:** in [`pipeline/stages/summarization/helpers/corpus_relate.py`](../pipeline/stages/summarization/helpers/corpus_relate.py), guard against the case where two rules in `all_rules` happen to share a `canonical_id` (impossible after B-001 fix, but cheap to assert).
* [ ] **B-002 audit:** for every paper in production, count how often `ContextAwareStitcher` had to absorb a phantom element pre-fix vs. post-fix. If the post-fix count is materially lower it strengthens the result in the thesis.
* [ ] **B-003 refactor:** reconsider the blanket `NodeScorer._ALWAYS_KEEP` exemption for `SECTION_HEADER`. R1 and R3 are safe to apply to headers; only R-color needs the exemption, and R-color is now disabled by default. Per-rule exemptions would be cleaner.
* [ ] **Summarisation throughput:** the cheap-tier MAP cascade hits the L3 escalation more than expected on multi-claim sentences. Worth profiling once cost-percentile sweep ([`scripts/estimate_pipeline_cost_percentiles.py`](../scripts/estimate_pipeline_cost_percentiles.py)) lands.
* [ ] **Thesis figure polish:** the `out/thesis_demo/ghost_text/*.png` crops are 110-dpi; bump to 200-dpi when finalising thesis figures so banner type stays sharp at print size.
* [x] **B-005 dedup:** the persistence + verbatim-from-DB helpers are line-for-line copies between `SummarizationRunner` and `BatchSummarizationRunner`. Lift into `pipeline/stages/summarization/persistence.py` as module-level functions taking `db` as a parameter, then point both runners at the shared impl. — **Shipped 2026-05-16.** Lifted 13 helpers (`build_rejection_summary` + `create_pipeline_run` / `finish_pipeline_run` / `clear_normalized_run_data` / `replace_verbatim_from_db` + 7 `persist_*` per-table writers + the `non_groupable_reason` already in `persistence.py`). Both runners now thin-wrap each as a 1-3 line forward. Net −421 LOC (runner.py 1946→1403, batch/runner.py 1830→1369, persistence.py 771→1354). All 14 `test_persistence.py` / `test_batch_persistence.py` cases pass. Full summarization suite identical pre/post (1 pre-existing flake in `test_phase_a_gate::test_normalize_stage_normalizes_entities`, unrelated). Cache/result helpers (`_pipeline_config_hash` / `_result_path` / `_load_result` / `_save_result`) intentionally **not** consolidated — see follow-up below.
* [ ] **B-005 follow-up — cache/result helper divergence:** the four cache/result helpers (`_pipeline_config_hash`, `_result_path`, `_load_result`, `_save_result`) were left as per-class methods during the B-005 dedup because the implementations diverge by design: (a) sync `_load_result` tags returned dict with `data["status"] = "skipped"` (B-008 fix); batch does not. (b) sync `_save_result` wraps the hash compute in try/except; batch does not. (c) sync `_pipeline_config_hash` reads `map_meta.run_metadata_summary()` first and includes `scorer` in the models dict; batch hardcodes the models dict from `self._l1`/`self._l2`/`self._l3`. (d) sync uses `self._cfg`; batch uses `self._cfg_full`. **Decision needed:** either backport B-008 / try-except wrappers / `map_meta` lookup to batch and unify (preferred long-term — restores parity), or split the contract formally and document that batch cache hits are not marked `skipped` in counters. Pre-work: confirm whether any batch caller reads `result["status"]` and expects `"skipped"`.
* [x] **B-005 end-to-end verification:** run `scripts/run_paper.py PMC<x> --sync=false` against a real DB with `db=get_db_connection()` wired in, confirm `sum_map_findings`/`sum_normal_findings`/…/`sum_rejection_summaries` get rows for `pipeline_run_id`, and spot-check `sum_map_findings.verbatim_support == text_element.text_content` for at least one finding. — **Verified 2026-05-16 against pipeline_runs 38–48 (5 papers, all `status='success'` in batch mode).** Result: **PARTIAL PASS.** Pass: `sum_normal_findings` / `sum_finding_groups` / `sum_canonical_rules` / `sum_final_rules` / `sum_rejection_summaries` / `sum_map_voter_outputs` / `sum_group_members` / `sum_normal_finding_spans` get rows; span-verbatim spot-check 5/5 exact match against `text_elements.text_content`; `persist_map_findings()` itself proven correct (direct call wrote 154 rows to run 48 from the on-disk batch handle). Fail: `sum_map_findings` shows 0 rows in 9 of 10 runs — see [Bug 55](BUGS.md#bug-55--sum_map_findings-not-populated-by-batch-runner). Tracked separately.
* [ ] **B-055 instrumentation + cache-short-circuit audit:** investigate why `sum_map_findings` is empty for batch-mode pipeline runs 38–48. (1) Add `logger.info("[%s] _persist_map_findings entering with %d chunks (%d findings) db_id=%s", ...)` at `pipeline/stages/summarization/batch/runner.py:522` (and the sync runner equivalent) so the next batch run leaves a paper trail. (2) Audit `BatchSummarizationRunner.finalize()` L472–L482 cache short-circuit: confirm whether `handle.cached_result_only=True` returns the cached `_load_result(...)` dict *before* any `sum_*` persistence runs while the `pipeline_runs` row already exists, which would explain the 0/0/100+ (map/normal/canonical) pattern on runs 47/46/44/43. (3) After instrumentation lands, run one paper end-to-end and confirm `sum_map_findings.COUNT(*) >= sum_normal_findings.COUNT(*)` for that `pipeline_run_id`. (4) Back-populate runs 38–47 from `out/summaries/batch_handles.prepatch/*.batch.json` via `persist_map_findings(db, run_id, pmcid, chunk_summaries)` once root cause is understood. Run 48 already manually back-populated (154 rows) as part of L36 verification.
* [x] **B-006:** decide policy on `SCOPE_QUALIFY` — emit it from `_classify_pair` or tear the plumbing out. — **Stripped 2026-05-14.** Enum value removed, RESOLVE filter / RELATE log column dropped; `FinalRule.scope_qualify_count` + DB column retained as hard-zero for back-compat.
* [x] **B-007:** stamp + check `pipeline_config_hash` in `SummarizationRunner._save_result` / `_load_result`. Strict parity with batch runner. — **Shipped 2026-05-14.** Added `_pipeline_config_hash()` on sync runner; hash now includes `enable_router` state on both runners; manifest builder reuses the same helper.
* [x] **B-008:** tag cache-hit return value with `status="skipped"` so `process_batch`'s skip counter is non-zero. — **Shipped 2026-05-14.** `_load_result` mutates the in-memory dict; on-disk JSON keeps `"success"`. Three downstream call-sites updated to treat `success ∪ skipped` as completed.
* [x] **B-009:** stop keeping per-paper state on `self._*` in `SummarizationRunner`. Either pop at end of `process()` or localise. — **Shipped 2026-05-14.** Pop in `finally` after the result dict is materialised; covers success / error / interrupt paths.
* [x] **B-010:** rewrite `filter_artifacts` to return indices (not filtered dicts) so `artifact_filter.py:59` rebuilds by identity, not dict equality. — **Shipped 2026-05-14.** Chose option 2 instead — built an `id()`-keyed `dict[int, LayoutElement]` lookup before the filter call. Public contract of `filter_artifacts` unchanged so `scripts/combined_pipeline.py` and `scripts/merged_pipeline.py` callers untouched. Two regression tests added in `tests/pdf_text_extraction/test_artifact_filter.py`.
* [x] **B-011:** either delete `ModelRegistry.docling_converter` or bring it up to parity with `DoclingLayoutExtractor._get_converter`. — **Shipped 2026-05-14.** Deleted the entire `ModelRegistry` class — zero in-tree consumers; each component already lazy-loads its own model (Docling via `DoclingLayoutExtractor`, TATR via `TATRTableDetector`, scispaCy via `summarization/umls_resources.get_nlp()`). `pipeline/stages/pdf_text_extraction/resources.py` removed; `__init__.py`, `docs/REPOSITORY_GUIDE.md`, `docs/STRUCTURE.md`, `.claude/CLAUDE.md` updated.
* [ ] **B-012:** rewrite `two_pass_extractor` header/footer strip in pure fitz coords, convert once with `BoundingBox.from_fitz_rect`.
* [x] **B-017:** wire `db=` through both batch entry-points in `scripts/run_paper.py` so `_corpus_relate_incremental` and per-paper `sum_*` persistence actually run under batch mode. — **Shipped 2026-05-15.** Extracted `_open_db_connection(caller_label)` helper; sync path now reuses it; both `build_batch_runner` call-sites pass its return value as `db=`.
* [ ] **B-015 propagation:** surface `raw_{relation_type,direction,category}` on `NormalFinding` / `FindingGroup` / `CanonicalRule` (and their DB tables) so audit trails survive past MAP. Currently only `sum_map_findings` has the raw columns.
* [ ] **B-015 audit query:** add a thesis-table query that buckets `(raw_relation_type, relation_type)` pairs across a corpus to expose systematic out-of-enum tendencies — useful evidence for "the enum is too narrow" or "X model prefers label Y".
* [ ] **B-015 confidence:** `Finding.confidence` is a strict `Literal` with no coercion; out-of-vocab values cause the whole finding to be dropped (`bad_findings.jsonl`). No raw column added because successful rows would always equal `confidence`. Revisit if we ever case-fold / coerce confidence.
* [ ] **B-016 DB backfill:** existing rows in `sum_map_findings.category` from pre-2026-05-14 runs still hold `"demographics"`. Either re-run MAP (default plan) or `UPDATE sum_map_findings SET category='demographic' WHERE category='demographics'`.
* [ ] **B-016 silver eval:** `eval/silver/prompts.py` still emits `"demographics"` (own `PROMPT_VERSION = v2`). Main pipeline's alias-repair handles it, but for cleanliness align + bump silver prompt to `v3` when next regenerating silver labels.
* [ ] **B-049 presentation-layer filtering:** after B-049, `unclear` / `no_direction` `FinalRule` rows appear in `sum_final_rules` and the inspector output. A future polarity-only view in `scripts/inspect_pipeline_output.py` (or any thesis-facing report) may want to hide `direction in {unclear, no_direction}` by default. **Filter at the presentation layer, not in RESOLVE** — the data pipeline must stay lossless so audit / RELATE-debug paths keep working.
* [ ] **ABC P1 — structured agreement:** extend agreement scoring beyond `Finding.claim` embedding similarity to a weighted combination of entity match, relation type, polarity, scope, evidence overlap, and semantic/NLI score. `HybridStructuredSimilarity` (in `pipeline/stages/summarization/agreement/hybrid_structured.py`) already covers category + claim-emb + entity + evidence; add explicit polarity + scope components and promote to default scorer via `SemanticAgreementScorer(strategy=HybridStructuredSimilarity(...))`. Detail and proposed weights in [`ABC_IMPLEMENTATION_COMPARISON.md` §7](ABC_IMPLEMENTATION_COMPARISON.md#7-proposed-agreement-score).
* [ ] **ABC P1 — lightweight comparison-normalisation in MAP:** before computing agreement, apply a cheap normaliser (case-fold + dictionary-snap polarity + category alias snap) so two voters saying the same thing in different surface forms don't escalate spuriously. Must not be persisted — full `NORMALIZE` still runs after MAP. Sketch in [`ABC_IMPLEMENTATION_COMPARISON.md` §6](ABC_IMPLEMENTATION_COMPARISON.md#6-recommended-target-design-for-our-pipeline).
* [x] **ABC P1 — hard-fail rules (polarity):** shipped 2026-05-15 as B-051. `AgreementChecker.compute` calls `detect_polarity_conflict` after the scorer but before theta — comparable findings with opposite `{positive, negative}` direction force `ChunkDecision.ESCALATE`; `MapOutputRouter._agreement_gate` emits `ReasonCode.POLARITY_CONFLICT` only (never co-emits low-agreement codes). v1 conservative: scope excluded from comparability; `absent`/`partial`/`unclear`/`no_direction` excluded from the hard-polarity set pending B-025 calibration. Cache invalidation via `MAP_SCHEMA_VERSION` + `MAP_AGREEMENT_POLICY_VERSION` (both caches covered). Evidence-disjoint hard-fail and scope-aware comparability deferred to v2. 11 regression tests + 3 hash regression tests. See [Bug 51](BUGS.md#bug-51--map-agreement-gate-treats-opposite-polarity-as-soft-disagreement).
* [ ] **ABC P1 — joint theta sweep:** extend `eval/silver/map_theta_sweep.py` to (i) sweep `(theta, reject_theta)` jointly, (ii) sweep alternate scorers (`SemanticAgreementScorer` over `EmbeddingSimilarityStrategy` vs. `HybridStructuredSimilarity`), and (iii) add a silver-judge precision column per row so we can pick a safe-deferral threshold instead of guessing `MapConfig.theta=0.8`. See [`ABC_IMPLEMENTATION_COMPARISON.md` §8](ABC_IMPLEMENTATION_COMPARISON.md#8-evaluation-plan).
* [ ] **ABC P1 — accepted-vs-L3 silver eval:** quantify the "is cascade safe?" question by replaying cached voter outputs through the silver judge for chunks where L1/L2 KEPT vs. chunks that escalated to L3. Reuse `eval/silver/` machinery. The cascade decision JSONL (`out/summaries/cascade_decisions/{pmcid}.jsonl`) gives the per-chunk acceptance level needed to bucket.
* [ ] **ABC P1 — pair-breakdown in reports:** surface `EmbeddingScorer.score_details.pairwise_upper` (already populated in `AgreementTrace`) into the HTML batch index and the cost report so disagreement cases can be audited without re-running MAP. Touches `scripts/templates/pipeline_batch_index.html.jinja2` and the trace/report glue in `pipeline/stages/summarization/observability/`.
* [ ] **B-018 follow-up — MAP prompt + schema audit:** work through the eight ranked issues in [`MAP_PROMPT_AUDIT.md`](MAP_PROMPT_AUDIT.md). Highest-impact items: (1) add `staging` + `molecular_genetics` relation_type examples to the MAP prompt (currently missing — LLM emits the category name as the relation_type, gets coerced to `unclear`, dropped at GROUP); (2) alias `treatment` → `treatment_response` (same-stem footgun as B-018); (3) log invalid `category` values structurally to `enum_observations.jsonl` instead of only `bad_findings.jsonl`; (4) tighten prompt to forbid null `subject_entity`/`outcome_entity` (currently invited by prompt, drops finding at GROUP). Bumping `MAP_PROMPT_VERSION` and `MAP_SCHEMA_VERSION` in a single commit re-uses one calibration run.
* [ ] **Calibration inventory — Tier 2 surfacing:** [`CALIBRATION_INVENTORY.md`](CALIBRATION_INVENTORY.md) §10 lists 11 HARD constants (LLM max_tokens / temperature / retries, masking sidebar/column heuristics, MIN_ANCHOR_H, evidence-gatherer pixel cutoffs, table-reconstructor multiplier, EmbeddingScorer alignment weights) that must move to YAML before any sweep can touch them. Coordinate with the zany-pnueli Tier 2 list — the two overlap on LLM hyperparams, UMLS threshold, NLI registry, and the masking duplicates.
* [ ] **Stage eval — model-agnostic battery:** implement the experiments described in [`STAGE_EVAL_EXPERIMENTS.md`](STAGE_EVAL_EXPERIMENTS.md) — MAP (M1–M3), GROUNDING (G1–G3), NORMALIZE (N1–N3), GROUP (GR1–GR3), CANONICALIZE (C1–C3), RELATE (R1–R4, gated on `raw_pairs.jsonl` being populated), RESOLVE (Rs1–Rs3), cross-run (X1–X5). Sequence per §5: structural replays → UMLS/embedding replays → NLI replays → cross-run alignment. Wire as `python -m eval.stage_experiments --run <RUN_ID> [--baseline <RUN_ID_B>]` emitting per-paper JSON + a run-level Markdown table. None of these require Opus silver, so the battery survives MAP cascade swaps.
* [x] **B-029 + B-038:** route `PipelineRunner._get_nlp` and `SummarizationRunner.load_paper_from_db` through `umls_resources.get_small_nlp()` instead of `spacy.load(...)`. — **Shipped 2026-05-15.** Added `get_small_nlp(model_name)` thread-safe per-model cache to `umls_resources.py`. Both call sites converted. Regression test `tests/summarization/test_scispacy_singleton.py` asserts `spacy.load(` only appears in `umls_resources.py` under `pipeline/stages/`. Honours `$NLP_HISTO_DISABLE_UMLS` for memory-constrained environments. Models stay distinct: small (`en_core_sci_sm`, ~40 MB) for sentence segmentation + entity-presence checks; large (`en_core_sci_lg`, ~750 MB + UMLS KB) for linking only — switching the small sites to large would waste ~700 MB everywhere with no benefit.
* [ ] **B-039 magnitude check:** before promoting severity, dump `[s["text_element_id"] for s in file_data["sentences_with_provenance"]]` for one real paper (e.g. `PMC7150310_main`) and confirm the te_id sequence cycles through `path_string`s rather than monotonically advancing within each path. Structural bug is unambiguous; empirical magnitude depends on Postgres heap-order alignment.
* [x] **B-039 fix:** flipped `load_paper_from_db` read site to `.order_by(TextElement.id)`. Works in tandem with the B-040 fix — `id` reflects ingest order, which is now document order. — **Shipped 2026-05-15.** Rejected the `document_order` column path: no partial-reingest workflow exists, so the column would be maintenance overhead with no payoff. Requires re-ingesting any papers whose `TextElement` rows were written pre-fix.
* [x] **B-040 fix:** replaced the global `by_path` defaultdict accumulator in `parsers/layout_utils.extract_text` with arrival-order traversal + contiguous-run stitching. Rows now emit in document order; sub-section returns no longer teleport the parent's later paragraphs to the parent's first-emit position. — **Shipped 2026-05-15.** Regression tests at `tests/pdf_text_extraction/test_extract_text_ordering.py`.
* [x] **B-041 fix:** `_run_voters` now returns a length-N None-padded list; `_cascade` builds `survivor_indices` + survivor list and passes both to `evaluate_chunk(..., voter_indices=...)`. `ChunkOutcome.valid_voter_indices` is the single source of truth for producer attribution — always original-spec-indexed. `producer_from_outcome` + `make_decision_record` rewritten to read it. Batch parser at `batch/runner.py:1414-1445` also rebuilt as None-padded. — **Shipped 2026-05-15.** Six regression tests at `tests/summarization/agreement/test_voter_index_alignment.py`. B-022 + B-023 shipped in the same session.
* [ ] **B-041 audit:** post-fix, sweep `out/summaries/runs/*/cascade_decisions/*.jsonl` from the calibration_set_v1 batch run for rows where `selected_provider/model` disagrees with the cost-report tokens-per-model breakdown. Non-zero count means the calibration cost figures need a regen footnote. Also check `logs/runA*.log` for "voter excluded from agreement" warnings as a proxy for how often the bug fired.
* [x] **B-042 fix:** reordered `_is_cut_off` so `_MID_SENTENCE_ABBREVS` check runs before the terminal-punctuation early-return; added a multi-token `last_two` join so `"Smith et al."` (final token `"al"`) matches the `"et al"` frozenset entry. Regression coverage in `tests/parsers/test_text_processing_cutoff.py` (21 cases). — **Shipped 2026-05-15.** See [BUGS.md Bug 42](BUGS.md#bug-42--is_cut_off-mid-sentence-abbreviation-rule-is-dead-code).
* [x] **B-043 fix:** restrict citation-number digit runs to `\d{1,3}` in `remove_citations` to avoid eating publication years. — **Shipped 2026-05-15** (matches [BUGS.md Bug 43](BUGS.md#bug-43--remove_citations-strips-publication-years) `Mitigated` status). All three citation-number branches in `parsers/text_processing.py` (lines 325, 328, 333) now cap digit runs at `\d{1,3}`. Regression test for `"Smith et al. 2020 reported."` survival still TODO if not already covered.
* [ ] **MAP design smell — `AgreementChecker.compute` single-voter shortcut:** `checker.py:61-62` auto-KEEPs a single-voter chunk with `confidence=1.0`. Diverges from `MapOutputRouter`'s `single_voter_policy="escalate"` default. Align both paths once the calibration data shows which policy is right.
* [ ] **MAP nit — `_make_chunks` overlap tail duplication:** `current_stages/map_stage.py:1203-1207` produces a redundant tail chunk when `chunk_overlap > 0` and total sentences don't divide evenly by stride. Break the comprehension once `i + chunk_size >= len(sentences)`. Dormant on the default `chunk_overlap=0`.
* [ ] **MAP design smell — `PipelineCache._input_hash` is content-blind:** `cache.py:87-90` hashes only the sorted multiset of `text_element_id`s. Stale cache hits possible if (a) sentence ordering changes for the same te_id set, or (b) `TextElement.text_content` is edited in-DB without changing the te_id. Mix a sentence-text hash into the key.
* [ ] **MAP design smell — empty voters report `pass_fraction=1.0`:** `map_stage._voter_grounding` and `semantic_scorer._pass_frac` return `1.0` for empty voters in the fallback path; router path correctly assigns `0.0`. Misleading in trace JSON. Return `0.0` everywhere; treat "empty" as "ungrounded".
* [ ] **MAP design smell — `SemanticAgreementScorer` honours `theta` but never emits REJECT:** `semantic_scorer.py:226-229`. Dormant because the runner doesn't pass `theta` directly to the scorer, but the asymmetry will bite the next refactor.
* [ ] **MAP design smell — `enable_router` default mismatch:** `SummarizationRunner.__init__` defaults `enable_router=False`, but `scripts/run_paper.py` flips it on so production is router-on. A new programmatic caller silently gets the legacy 3-tier cascade. Align defaults.
* [ ] **MAP prompt nit — citation format inconsistency:** `prompts.py:22` says `[S1|PMC123456|789]` (with brackets) while the JSON schema example at line 217 shows `S1|PMC123456|789` (without). Current logs show zero `INVALID_SENTENCE_ID` from this — clarity nit, not silent voter loss. Drop the brackets from line 22.
* [ ] **B-044 follow-up — mine `cross_field_bleed` counts:** after the next calibration run grep `logs/enum_observations.jsonl` for `reason="cross_field_bleed"` and split counts by `coerced_value`. Any non-trivial `coerced_value="unclear"` bucket (esp. `raw_value="staging"`) escalates to a claim-aware repair stage. Track on `MAP_PROMPT_AUDIT.md` issue list.
* [ ] **B-044 longer-term — collapse `expression` + `has_feature` into one bucket:** `category` already carries the assay-type signal (`IHC` / `morphology` / `molecular_genetics`); the relation-level split is fuzzy (mutations land in `expression`, translocations in `has_feature` purely by prompt convention) and is a recurring source of cross-field bleed. Refactor needs (a) enum collapse in `RelationTypeEnum`, (b) cache invalidation across `sum_map_findings` / `sum_normal_findings` / `sum_canonical_rules`, (c) GROUP key audit, (d) downstream RELATE/RESOLVE consumer audit. Defer until step-2 alias data justifies the churn.
* [ ] **B-024 follow-up — Option B (split + migrate):** B-024 is mitigated (docstring-only, 2026-05-15) but the user-facing reader-confusion symptom persists — field names, DB column names, and inspector labels still surface the value without label context. Full fix: (a) add `nli_entailment_a_to_b` / `nli_contradiction_a_to_b` (and `b_to_a` pair) to `Relation` in `pipeline/stages/summarization/models.py`; (b) write `relate_stage._classify_pair` to populate both column pairs unconditionally from `RawNLIPair`; (c) Alembic migration to add columns and backfill from `sum_raw_nli_pairs` on existing rows; (d) deprecate the polysemic `nli_score_*` columns (keep one cycle for backreaders); (e) relabel `scripts/run_paper_single_model.py:405` and `scripts/inspect_pipeline_output.py:127-128, 409-410, 798`. Pre-work: grep `scripts/`, `eval/`, notebooks for any consumer treating `nli_score_*` as a pure entailment score (Option C's safety condition was not audited at mitigation time). Verification per [Bug 24](BUGS.md#bug-24--relationnli_score_-field-doc-disagrees-with-relate_stage-write-path).
* [ ] **Hardcoded-values audit (2026-05-16):** sweep found hardcoded values scattered across the codebase that arguably belong in config. Worst offenders, in priority order:
  1. **LLM model IDs** — `pipeline/stages/summarization/voter_configs.py:19-56` bakes L1/L2/L3 tier model names (`"DeepSeek-V3.2-Speciale"`, `"gemini-2.5-flash-lite-preview-06-17"`, `"mistral-large-3"`, `"kimi-k2.5"`, `"claude-haiku-4-5-20251001"`, `"claude-sonnet-4-6"`) into Python. Same names duplicated in `llm_providers.py:20-26` and `summarization/runner.py:19-28` docstrings. Externalize to `config/models.yaml`.
  2. **Request timeouts + retries hardcoded in fn signatures** — `llm_providers.py:127,187,243,267,287` (20/60/180s per provider), `agreement/providers.py:74,156` (`range(6)` retries, 5→120s backoff), `agreement/providers.py:17,94` (`OPENAI_BATCH_SIZE=512`, `GEMINI_BATCH_SIZE=100`).
  3. **Agreement scoring weights duplicated across files** — `agreement/composite.py:43-45` (`keep_emb=0.80`, `keep_ner=0.50`, `reject=0.20`), `agreement/embedding.py:26,31,304-307` (`_NEG_WINDOW=3`, `_NUMERIC_RATIO_THRESHOLD=2.0`, `tau=0.15`, `count_alpha=0.25`, `reuse_weight=0.15`, `contradiction_weight=0.20`). Same weights re-declared in `hybrid_structured.py:87-90`. Centralize to `config/agreement_weights.yaml` + dedupe.
  4. **Model names outside config** — `pdf_text_extraction/runner.py:162,318` (`"en_core_sci_sm"` literal), `pdf_text_extraction/config.py:105` (`"microsoft/table-transformer-detection"`), `parsers/pdf_parsers/nougat_parser.py:40` (`"facebook/nougat-base"`). Add env-var fallbacks.
  5. **Output paths scattered** — `pdf_text_extraction/config.py:37-52` has 10+ `Path("out/...")` defaults; `summarization/runner.py:30` and `helpers/corpus_relate.py:51-52` duplicate more. Add `OUTPUT_ROOT` env var unifying all roots.
  6. **PDF layout magic numbers** — `pdf_text_extraction/config.py:135,138,140-141` (figure-size + proximity thresholds), `:106,134,209,213` (DPI=150 vs 200 in different sections — no doc why), `:84` (Docling `timeout_sec=300`). UMLS threshold `umls_utils.py:11` (`UMLS_THRESHOLD=0.85`) hardcoded at module level.
  7. **Misc** — `db_connection.py:28-30` (`'localhost'`/`'5432'`/`'nlp_histo'` defaults — acceptable), `batch_ner.py:112` (`min_chars=50`, `save_interval=50`), `count_tokens.py:71` (`chunk_size=10`).
* [x] **B-027 remaining — `seed` and `skip_existing_outputs`:** Shipped 2026-05-15. `_seed_pipeline()` seeds `random` / `numpy` / `torch` (+ `torch.cuda` when available) at `PipelineRunner.__init__`; `seed` widened to `int | None` so callers can opt out. `skip_existing_outputs` is per-stage (not whole-paper sentinel) — new `_StageCache` (`stage_cache.py`) caches stages 2/5/6 with config-hash sidecars; final writers (steps 7/8) and Docling extraction (steps 1/4) still run. Determinism is scoped to pipeline-owned RNGs only; external libraries (Docling/TATR/OCR/scispaCy) not covered. See [BUGS.md Bug 27](BUGS.md#bug-27--runtimeconfig-knobs-num_workers-log_level-seed-skip_existing_outputs-not-consumed) and [HOW_TO_RUN.md §9](HOW_TO_RUN.md#9-per-stage-output-cache-b-027). 22-test regression in `tests/pdf_text_extraction/test_b027_seed_and_cache.py`.

---

## Decisions log

Permanent design calls — keep terse, link to the discussion in the detail section.

| Date | Decision | Rationale | Detail |
|------|----------|-----------|--------|
| 2026-05-13 | `group_id` includes `pmcid` in its hash. | Prevents cross-paper `canonical_id` collisions; cross-paper matching uses the dedicated CUI / normalised-string gate in `corpus_relate.py`, not id equality. | [Bug 1](BUGS.md#bug-1--duplicate-intra-paper-relations-produced-by-canonical_id-collisions) |
| 2026-05-13 | `TwoPassConfig.enabled = True` by default. | R1 (pixel render) reliably catches Docling phantom elements and PDF render-mode-3 / fill-opacity-0 ghost text. The stitcher no longer has to be the only line of defence. | [Topic — ghost-text detection](BUGS.md#topic--ghost-text-detection-empirical-verification-and-policy-fix) §2.3 |
| 2026-05-13 | `TwoPassConfig.max_white_char_fraction = 1.0` (R-color disabled). | Empirically produces false positives on legitimate inverted headers; R1 is the source of truth. Re-enable per-corpus only if R1 is unavailable. | [Bug 3](BUGS.md#bug-3--r-color-white-text-false-positive-latent) |
| 2026-05-13 | Don't add a pdfminer.six render-mode pass. | R1 already catches Tr=3 and opacity-0 as "no ink in bbox" — verified synthetically (`scripts/verify_ghost_text_detection.py`). A second detector would be duplicated work. | [Topic — ghost-text detection](BUGS.md#topic--ghost-text-detection-empirical-verification-and-policy-fix) §2.1 |
| 2026-05-14 | Batch runner brought to parity with sync runner. | Six features (verbatim-from-DB, stable `finding_id`, DB persistence, `corpus_relate_incremental`, `rejection_summary`, NER) were sync-only; production batch runs were grounding against paraphrased text. Copied the methods directly into `batch/runner.py` rather than refactoring sync to keep blast radius small. | [Bug 5](BUGS.md#bug-5--batch-runner-missing-sync-parity-features) |
| 2026-05-14 | Batch result caching uses `pipeline_config_hash` invalidation. | Stamping the result JSON with the current config hash + checking it at `submit()` is the cheapest correct cache key. Any threshold/model/schema/prompt change auto-invalidates without manual cleanup. Pre-fix JSONs in `out/summaries/summaries/` were deleted to force regeneration. | [Bug 5](BUGS.md#bug-5--batch-runner-missing-sync-parity-features) |
| 2026-05-14 | Default MAP scorer switched to `SemanticAgreementScorer(EmbeddingSimilarityStrategy)`. | Centrality-based best-output selection (Soiffer 2025). Previous default `EmbeddingScorer` left `bundle.best_index` unset, so `AgreementChecker.best()` fell back to a `(mean_evidence_length, n_findings)` heuristic — which can be gamed by a voter that copies the whole paragraph as evidence. | [`ABC_IMPLEMENTATION_COMPARISON.md` §5 Gap 1](ABC_IMPLEMENTATION_COMPARISON.md#gap-1-no-centrality-based-output-selection-on-default-path) |
| 2026-05-14 | `MapOutputRouter` wired into both runners by default. | Schema + provenance validation runs before agreement; unusable voters drop out before scoring instead of inflating the agreement matrix with fabricated content. Router path escalates L1 → L3 directly because L2 voters are the same risk class as L1. Opt-out via `enable_router=False`. | [`ABC_IMPLEMENTATION_COMPARISON.md` §5 Gap 2 + Gap 8](ABC_IMPLEMENTATION_COMPARISON.md#gap-2-grounding-does-not-gate-the-cascade) |
| 2026-05-14 | Per-level cascade decision factored into `agreement/decision.py::evaluate_chunk`. | Sync (`MapStage._cascade`) and batch (`BatchSummarizationRunner._process_level`) previously re-implemented the KEEP/escalate decision inline — a recurring source of drift. Both runners now call the same function, so a paper processed in sync vs batch takes the same cascade path by construction. | [`ABC_IMPLEMENTATION_COMPARISON.md` §5 Gap 3](ABC_IMPLEMENTATION_COMPARISON.md#gap-3-sync-and-batch-run-different-cascade-code) |
| 2026-05-14 | Per-chunk cascade decision log: one JSONL row per L1/L2/L3 decision. | `CascadeDecisionLog` writes to `output_dir/cascade_decisions/{pmcid}.jsonl` independently of `trace_enabled`. Enables offline aggregation of acceptance rate, gate origin, reason codes, and selected provider/model per chunk — needed before any theta sweep can claim "safe deferral". | [`ABC_IMPLEMENTATION_COMPARISON.md` §9 P0 task 4](ABC_IMPLEMENTATION_COMPARISON.md#p0--must-have-before-expensive-experiments) |
| 2026-05-15 | Alias-repair `relation_type="prognosis"` → `"prognostic"` rather than rename the enum value. | Renaming `RelationTypeEnum.prognostic` → `prognosis` to align with the `category` field's noun form would touch five downstream Pydantic models (NormalFinding, FindingGroup, CanonicalRule, Relation, FinalRule) plus an Alembic migration over historical `sum_*` rows. Alias repair is a one-line dict + validator branch; the raw value is still preserved on `Finding._raw_relation_type` (per B-015) and emitted to `logs/enum_observations.jsonl`, so we can still measure alias frequency and revisit the rename later if needed. `category="expression"` deliberately left unaliased — collapsing it to `IHC` would lose `molecular_genetics` claims; the LLM's misuse is signal for a prompt fix, not a silent mapping. | [Bug 18](BUGS.md#bug-18--relation_type-prognosis-noun-form-coerced-to-unclear-instead-of-prognostic) |
| 2026-05-15 | Single-file unified config via `configs/run.yaml` + `pipeline/config_loader.load_config()`. | Loader maps a nested YAML onto both `PipelineConfig` and `SummarizationConfig` in one call. Recursive dataclass mapping, Enum-from-string coercion, `Path` wrapping, strict unknown-field rejection. Partial overrides keep dataclass defaults — caller only specifies what differs from the default. Tests at `tests/test_config_loader.py`. Surfaces dead-knob bugs (B-027 seed/skip-existing) as ghost knobs the YAML can set but the runner ignores — comments in `run.yaml` mark them. Cluster bundle (B-030, B-031, B-032, B-034, B-037) cleared 2026-05-15: knobs either deleted or wired (TATR `render_dpi`, summarization `extra_synonyms`). | [`pipeline/config_loader.py`](../pipeline/config_loader.py) + [`configs/run.yaml`](../configs/run.yaml) |
| 2026-05-15 | `MaskingConfig.merge_iou_threshold` deleted, not wired. | `merge_rects` (`parsers/layout_utils.py:103`) uses any-intersection union — no IOU concept exists in the algorithm. The field was misnamed leftover. Adding IOU logic would be a behaviour change to every `merge_rects` caller (masking, table reconstruction, hybrid detection), disguised as a config wire. Tightened `merge_rects` docstring to spell out the any-intersection semantics. | [Bug 33](BUGS.md#bug-33--maskingconfigmerge_iou_threshold-never-passed-to-merge_rects) |
| 2026-05-15 | NLI model / batch / device knobs surfaced via `configs/nli_models.yaml` + `nli_config.get_active_spec()`, not via `GroundingConfig` / `RelateConfig` fields. | Keeps NLI-specific knobs out of the calibration-thresholds dataclass and lets a corpus run pin a specific NLI build alongside model versions without re-running Python config construction. Switching the active spec changes both `GroundingFilter` and `RelateStage` at next run with zero code edits. If a future need to override per-run from Python emerges, layer it on top by threading `model_name=` through `runner.py:251/258`. | [Bug 36](BUGS.md#bug-36--groundingfilter--relatestage-modelbatchdevice-not-exposed-via-summarizationconfig) |
| 2026-05-15 | Document order propagated via `extract_text` arrival-order emission + `ORDER BY id` read site, not a new `document_order` column. | `db_ingester.write` iterates `rows` and `session.flush()` per row, so autoincrement `id` reflects insertion order. Once B-040 emits `rows` in document order, `id` reflects document order. `PostgresDatabaseIngester` short-circuits if `Document` exists, so partial reingest is not a supported path — a dedicated column would be maintenance overhead without payoff. Re-ingest needed for any paper whose rows were written pre-fix. | [Bug 39](BUGS.md#bug-39--load_paper_from_db-orders-by-position_in_section-only-interleaves-sections) + [Bug 40](BUGS.md#bug-40--extract_text-emits-paragraphs-in-path-first-appearance-order-not-document-order) |
| 2026-05-15 | Extend `_RELATION_TYPE_ALIASES` with `morphology→has_feature`, `ihc→expression`, `molecular_genetics→expression`; tag category-name bleed with `reason="cross_field_bleed"`. | MAP voters bleed `category` values into `relation_type` ~10× per run, dropping findings at GROUP (`unclear` is non-groupable). The three new aliases are unambiguous in this codebase (`morphology` is always morphological, `IHC` is always a staining readout, `molecular_genetics` follows the prompt's variant-call=expression rule). `staging` deliberately left unaliased — descriptive (`has_feature`) vs outcome-driven (`prognostic`) split needs claim context, so it falls to `unclear` with a `cross_field_bleed` tag for measurement. Chose this over collapsing `expression`+`has_feature` because the bigger refactor needs cache + downstream audit and the alias map clears most of the loss at near-zero risk. | [Bug 44](BUGS.md#bug-44--map-relation_type-bleeds-category-names-and-loses-findings-at-group) |
| 2026-05-15 | `ChunkOutcome.valid_voter_indices` is the single source of truth for original-spec voter indices; `RoutingDecision.valid_voter_indices` stays survivor-indexed (router-internal). | Two `valid_voter_indices` fields previously coexisted with diverging semantics; downstream consumers (`producer_from_outcome`, `make_decision_record`) read from the router's view and silently misattributed whenever a voter failed (B-041). Mapping back to original indices inside `evaluate_chunk` keeps router internals encapsulated and gives the public `ChunkOutcome` a clean original-spec contract. Reject alternative: mutate `RoutingDecision.valid_voter_indices` post-router — gross to mutate the router's output from outside. | [Bug 41](BUGS.md#bug-41--producer-attribution-mis-indexed-when-any-voter-fails) |
| 2026-05-15 | GROUP bucket key is the normalized entity STRING only; CUI never participates in the per-paper hash. | `NormalizeStage._resolve_entity` is string-deterministic given the synonym dict, but the auxiliary UMLS lookup that fills the CUI is *not* — short acronyms link inconsistently across runs (no in-document context for the linker), so mixing CUI with string in the key split otherwise-identical findings whenever CUI population was partial (B-022). CUI rides along on `NormalFinding.{subject,outcome}_cui` for downstream cross-paper matching at `corpus_relate` time. | [Bug 22](BUGS.md#bug-22--group_id-mixes-cui-and-string-keys-when-cui-population-is-partial) |
| 2026-05-15 | NORMALIZE dedup key includes `direction`. | Pre-fix, opposite-polarity claims from the same sentence collapsed into one merged finding, erasing the contradiction before RELATE could see it. Adding `direction` to the key keeps disagreement intact at the cost of marginally inflated rule counts when one voter emits `unclear`/`None` while another emits a concrete direction — the direction-from-claim heuristic (`infer_direction`) in `_normalize_entities` resolves most of those cases to the same concrete direction before dedup runs, so the inflation hazard is small in practice. Picked over Option B (direction-aware merge that emits one finding per direction within a same-(te_id, subj, out, rel) bucket) because Option A is the same behaviour with simpler code. | [Bug 23](BUGS.md#bug-23--normalize-dedup-collapses-opposite-direction-findings-from-the-same-sentence) |
| 2026-05-15 | MAP agreement gate hard-fails on comparable opposite-polarity findings in `AgreementChecker`, with both caches version-bumped. | Located the veto at `AgreementChecker.compute` (post-scorer, pre-theta) rather than in the scorer or only in the router so router-path, no-router-path, and batch-path share one chokepoint. v1 polarity set is `{positive, negative}` only — `absent`/`partial` deliberately excluded because their semantics overlap with `negative`/`positive` respectively in this domain (RELATE already bundles `partial` with positives; B-025 owns the calibration call). v1 comparability mirrors `group_stage._group_id` (4 structural fields, no scope) — accepts cross-cohort false-escalates over missed contradictions. Cache invalidation: `MAP_SCHEMA_VERSION` bump covers the chunk-level `PipelineCache`; new `MAP_AGREEMENT_POLICY_VERSION` in `compute_pipeline_config_hash` covers the per-paper result cache. Evidence-disjoint hard-fail deferred — paraphrases citing different valid sentences would false-escalate without a proximity policy. | [Bug 51](BUGS.md#bug-51--map-agreement-gate-treats-opposite-polarity-as-soft-disagreement) |
| 2026-05-15 | `FindingScope.scope_parsed` recomputed by a `mode="after"` validator; LLM-emitted value ignored. | Trivially derivable boolean (`any(sub_field is not None)`). Keeping it LLM-set burned output tokens and gave the model one more thing to get wrong. Field stays in the Pydantic schema because OpenAI strict mode requires every property; prompt now tells voters to always emit `false`. | [Bug 45](BUGS.md#bug-45--scope_parsed-is-llm-set-but-trivially-derivable) |
| 2026-05-15 | `_DIRECTION_ALIASES` maps hedging words (`maybe`/`possibly`/…) to `unclear`, natural-language nulls (`none`/`n/a`/`na`) to `no_direction`. | Same alias-repair shape as `_RELATION_TYPE_ALIASES` (B-018, B-044) — turns silent "unknown" telemetry into a labelled `alias_repair` bucket so we can mine hedging frequency. Audit deferred this on a 1-occurrence threshold; we shipped because the fix is a 5-line dict + branch matching a well-trusted pattern. | [Bug 46](BUGS.md#bug-46--direction-hedging-words-coerce-to-unclear-instead-of-alias-repair) |
| 2026-05-15 | Prompt-only disambiguation: `direction=absent` reserved for `expression`-class "negative staining" / "no expression detected"; `direction=negative` for "decreased / reduced expression" and all non-expression relation_types. | The two labels were both valid for the same expression claim, and the RELATE polarity guard treats them as different polarities — blocking CONTRADICT on findings that should pair. Constraining the rubric by relation_type is cheaper than splitting the enum. Empirical validation deferred to the next calibration run. | [Bug 47](BUGS.md#bug-47--direction-absent-vs-negative-ambiguity-on-expression-claims) |
| 2026-05-15 | `Rule.type` and `RuleCounts` field names lowercased to match `Finding.confidence` / `Finding.category` convention; `mode="before"` validator preserves back-compat for cached Title-Case payloads. | Consistency over casing across the summarisation enums removes one footgun and one open invitation to a future casing-alias map. RULE block is off by default so no DB migration is needed; the back-compat validator covers any cached `ExtractedRules` JSONs. | [Bug 48](BUGS.md#bug-48--ruletype-title-case-inconsistent-with-lowercase-convention) |
| 2026-05-15 | Docling per-document timeout enforced via thread + `Future.result(timeout=)`, not subprocess. | Subprocess isolation is the textbook fix but the converter is stateful (model load per call) and `LayoutResult` doesn't pickle cleanly. Thread + abandoned-worker is cheaper and the runner already blacklists the pmcid on the raised `TimeoutError`. The leaked thread costs less than a hung batch. | [Bug 35](BUGS.md#bug-35--doclingconfigtimeout_sec-never-enforced-by-doclinglayoutextractor) |
| 2026-05-15 | `remove_citations` digit-length cap (`\d{1,3}`) rather than year-shape negative lookahead. | Both fixes work; the digit-cap version surfaces clearly in `git diff` and side-steps the en-dash range edge case (`1999–2003` would falsely match a year-shape lookahead applied per-token). Three-digit cap permits citation numbers up to 999 — well above the largest citation count in modern pathology papers. | [Bug 43](BUGS.md#bug-43--remove_citations-strips-publication-years) |
| 2026-05-15 | CANONICALIZE emits one `CanonicalRule` per observed direction (no folding); `is_conflicted` repurposed to a group-level signal; RELATE / corpus_relate skip pairs where either side carries `unclear` / `no_direction`. | The old folding policy converted hedged findings into pseudo-votes for the majority direction (honesty hole) and created a non-deterministic tie-break (reproducibility hole, supersedes B-026). Per-direction binning eliminates both. Pipeline stays lossless — unclear / no_direction rules flow through RESOLVE → FinalRule for traceability but are inert in the relation graph. `partial` semantic question deferred to B-025 (separate calibration sweep). Cache invalidation: `CANONICALIZE_DIRECTION_POLICY_VERSION` fed into `pipeline_config_hash`. | [Bug 49](BUGS.md#bug-49--canonicalize-folds-unclear--no_direction-into-majority-polarity-bin) |
| 2026-05-15 | Deleted `DatabaseConfig.{schema, create_tables_if_missing, batch_size, connect_timeout_sec}` rather than wiring. | Four fake knobs with zero consumers (`PostgresDatabaseIngester` only accepts `db_url`). No current thesis demand for schema isolation, configurable connect timeouts, or custom insert batching; `create_tables_if_missing` and `batch_size` would each escalate into deeper DB-layer refactors (ORM `MetaData` rebinding, transactional batching) disproportionate to the use case. Strict-unknown-key validation in `pipeline/config_loader.py:48-52` now rejects YAMLs referencing the removed fields (regression test `test_deleted_database_keys_rejected`). If a real consumer surfaces later, reintroduce the field with a real implementation and a behaviour-asserting test. | [Bug 28](BUGS.md#bug-28--databaseconfig-sub-fields-never-propagated-to-postgresdatabaseingester) |
| 2026-05-15 | Tier 1 config audit: small, non-semantic PR only. Tier 2 (UMLS / NLI / LLM hyperparams / `cascade_profile` / env-override-layer formalisation / config-hash expansion / layout-constant cleanup) explicitly deferred. | Each Tier-2 knob changes pipeline outputs and needs its own pipeline-config-hash + run-artifact reproducibility test. Bundling them with dead-config purges would muddy review and make regression bisection harder. Phase-1 audit also incorrectly flagged `RuntimeConfig.{blacklist_if_rows_exceed, multi_source_crops}` as dead — verification at implementation time caught working consumers in `runner.py:375-378, 603+` (kill-switch features, defaults off). Both kept. | [Bug 28](BUGS.md#bug-28--databaseconfig-sub-fields-never-propagated-to-postgresdatabaseingester), [Bug 50](BUGS.md#bug-50--poll_interval-default-mismatch-across-cli-and-batch-helpers) |
| 2026-05-15 | `scripts/run_paper.py` poll-interval default consolidated onto module-level `DEFAULT_POLL_INTERVAL_SEC = 60`. | Three diverging defaults (CLI=60, `_run_all_batch`=20, `_run_batch`=60) silently confused programmatic callers depending on which helper they imported. Single-source-of-truth constant fixes the drift class; `tests/test_poll_interval_defaults.py` pins it via `inspect.signature` so a future addition of a positional parameter cannot mask the regression. | [Bug 50](BUGS.md#bug-50--poll_interval-default-mismatch-across-cli-and-batch-helpers) |
| 2026-05-16 | Calibration-set selection: three buckets (`related` / `diverse` / `hard`), ILP as the documented default, greedy as fast fallback. | Three buckets target three independent failure modes of the summarisation pipeline (cross-paper RELATE coverage, entity-space coverage, individually-difficult papers). ILP gives a globally optimal pick under stated objective + constraints — defensible position in the thesis. Greedy farthest-point stays for fast smoke runs but is order-sensitive at the margins. PuLP + bundled CBC chosen over Gurobi/CPLEX (no licence) and solves at corpus scale within the per-bucket 30s budget. Algorithm + formulas + design rationale: [`docs/readmes/PAPER_SELECTION.md`](readmes/PAPER_SELECTION.md). | [`eval/paper_selection/`](../eval/paper_selection/) |
| 2026-05-16 | Adopt a no-API local eval harness; ship Phase 1 (proxy metrics) first. | Frozen-artifact analysis is the only thesis-defensible way to compare summarisation configurations without spending API budget. Phase 1 (`scripts/eval/compute_proxy_metrics.py`) is pure file I/O: no NLI, no LLM imports, no pipeline re-execution. Later phases (manual sampling, silver labels, frozen-artifact threshold analysis, downstream replay, config comparison) are designed but explicitly deferred so the first PR is small and reviewable. Replay/sweep over `theta`/`reject_theta` further gated on a future producer-side change that persists per-voter MAP outputs — currently the cache stores only cascade-resolved per-chunk results. | [`docs/CALIBRATION_EVAL.md`](CALIBRATION_EVAL.md) |
| 2026-05-16 | Calibration knob inventory lives in `docs/CALIBRATION_INVENTORY.md`, organised by stage with one row per knob (ID, default, range, wired-via, what it affects, measurement target). | Separates "what can we vary in an experiment" (this inventory) from "is config plumbing clean" (zany-pnueli config audit). Built fresh from configs because earlier-revision Appendix-A audit referenced in plan `i-want-you-to-reactive-quiche.md` lives only in `/Users/emir/.claude/plans/` git history, which is not a real repo — that source data is unrecoverable. New knobs land as a row in the matching section with a pointer to either a `compute_proxy_metrics` column or an experiment ID in `eval/EXPERIMENTS.md` / `docs/STAGE_EVAL_EXPERIMENTS.md`; HARD-status rows feed §10 surfacing backlog. | [`docs/CALIBRATION_INVENTORY.md`](CALIBRATION_INVENTORY.md) |
| 2026-05-16 | Per-voter MAP persistence: sync path verified end-to-end; batch path is the open gap. | Phase 0 audit per [`CALIBRATION_EXECUTION_PLAN.md`](CALIBRATION_EXECUTION_PLAN.md) §10. Sync `SummarizationRunner._persist_voter_outputs` (`runner.py:1109-1153`) writes one `SumMapVoterOutput` row per (chunk, level, voter_index); `PipelineCache.set_map(..., voter_outputs=...)` round-trips per-voter rows across cache hits; `MapStage` rebuffers cached rows so `sum_map_voter_outputs` populates even on cache hit. `BatchSummarizationRunner.finalize()` has no `_persist_voter_outputs` method and no `_buffer_voter_outputs` call site — code-wise, batch runs do not write per-voter rows or cache them. B-055 evidence claiming `sum_map_voter_outputs` populated on batch runs 38–48 contradicts this and is treated as a data point requiring runtime re-verification on a paper never previously processed via sync (see B-056). Sweep path A (sync + DB replay) and path B (`eval/silver/map_theta_sweep.py` standalone primer) are both safe today; sweep over batch-only papers is not. | [Bug 56](BUGS.md#bug-56--batch-runner-omits-per-voter-map-persistence-code-path-absent), [`docs/CALIBRATION_EXECUTION_PLAN.md`](CALIBRATION_EXECUTION_PLAN.md) |
